#!/usr/bin/env bash
options="/clipboard -grab-mouse -grab-keyboard +dynamic-resolution"
file=
if [[ "$1" != -* ]] && [[ "$1" != +* ]] && [ -e "$1" ]; then
    file="$1"
    shift
fi
eval xfreerdp3 "$file" "$options" "$@"
