#!/usr/bin/env python
import sys
import urllib.parse as ul
import argparse
parser = argparse.ArgumentParser()
parser.add_argument("--safe", default="")
args = parser.parse_args()

print(ul.quote(sys.stdin.read(), safe=args.safe), end='')
