#!/bin/bash
# Update tools installed by package managers of different languages

# Python
pip list --outdated --format=freeze | grep -v '^\-e' | cut -d = -f 1  | xargs -n1 sudo pip install -U
# PHP
composer global u
# Nodejs
yarn global upgrade 
# Rust
rustup update
